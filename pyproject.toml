[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "orglens"
version = "0.2.0"
description = "Organizational lens for AI agents — units of work, and the sessions that belong to them"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "pyyaml>=6.0",
]

[project.scripts]
orglens = "orglens.cli:cli"

[tool.pytest.ini_options]
testpaths = ["tests"]
